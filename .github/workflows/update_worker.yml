name: auto update and deploy

on:
  schedule:
    - cron: "0 16 1 * *" # 每天 00:00 北京时间 (UTC+8)
  workflow_dispatch: # 允许手动触发

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: write
      issues: write

    env:
      GITHUB_REPO: bia-pain-bache/BPB-Worker-Panel
      # 更新版本类型 默认正式发布版本 release,prerelease为预发布版本
      RELEASE_TYPE: release
      # KV 名称
      KV_NAME: presoak-kv

    steps:
      - name: Checkout 仓库 # 检出仓库代码
        uses: actions/checkout@v6

      - name: 设置 Node.js 最新稳定版 # 设置 Node.js 最新稳定版
        uses: actions/setup-node@v6
        with:
          node-version: "lts/*"

      - name: 安装依赖
        run: |
          sudo apt-get update
          sudo apt-get install -y jq curl unzip

      - name: 获取最新版本号
        id: get_last_version
        run: |
          # 获取更新类型，默认为 'release'
          ACTUAL_RELEASE_TYPE="${{ env.RELEASE_TYPE || 'release' }}"
          echo "更新类型: $ACTUAL_RELEASE_TYPE"

          if [ "$ACTUAL_RELEASE_TYPE" == "prerelease" ]; then
            # 如果选择预发布版本，获取所有 release 并筛选最新的非草稿预发布版本
            latest_tag=$(curl -s https://api.github.com/repos/${{ env.GITHUB_REPO }}/releases \
              | jq -r 'map(select(.prerelease == true and .draft == false)) | sort_by(.published_at) | .[-1].tag_name')
            if [ -z "$latest_tag" ]; then
              # 未找到任何预发布版本。
              echo "未找到任何预发布版本。" 
              exit 1
            fi
            # 最新预发布版本
            echo "最新预发布版本: $latest_tag"
          else
            # 默认或选择正式发布版本，获取最新正式发布版本 (GitHub API 的 /latest 默认返回非预发布版本)
            latest_tag=$(curl -s https://api.github.com/repos/${{ env.GITHUB_REPO }}/releases/latest | jq -r '.tag_name')
            if [ -z "$latest_tag" ]; then
              # 未找到任何正式发布版本。
              echo "未找到任何正式发布版本。" 
              exit 1
            fi
            # 最新正式发布版本
            echo "最新正式发布版本: $latest_tag"
          fi
          # 将最新标签名和实际选择的发布类型设置为环境变量，供后续步骤使用
          echo "latest_tag=$latest_tag" >> $GITHUB_ENV
          echo "release_type=$ACTUAL_RELEASE_TYPE" >> $GITHUB_ENV

      - name: 读取当前版本号
        id: read_version
        run: |
          if [ -f "version.txt" ]; then
            current_version=$(cat version.txt | tr -d '\n' | tr -d '\r')
          else
            current_version=""
          fi
          echo "当前版本: $current_version"
          echo "current_version=$current_version" >> $GITHUB_ENV

      - name: 判断是否更新
        id: check_update
        run: |
          if [ "$current_version" = "$latest_tag" ]; then
            echo "need_update=false" >> $GITHUB_ENV
            echo "无需更新"
          else
            echo "need_update=true" >> $GITHUB_ENV
            echo "需要更新"
          fi

      - name: 下载并替换 _worker.js
        # 仅在需要更新时执行此步骤
        if: env.need_update == 'true'
        run: |
          # 创建临时目录用于操作
          TEMP_DIR=$(mktemp -d)
          echo "创建临时目录: $TEMP_DIR"

          # 进入临时目录
          cd "$TEMP_DIR"

          # 构建下载 URL
          download_url="https://github.com/${{ env.GITHUB_REPO }}/releases/download/${{ env.latest_tag }}/worker.js"
          echo "下载文件: $download_url"
          # 使用 curl 下载文件并保存为 _worker.js
          curl -L -o _worker.js "$download_url"
          # 验证 _worker.js 是否下载成功
          if [ ! -f "_worker.js" ]; then
            echo "_worker.js 下载失败或文件为空"
            exit 1
          fi
          echo "_worker.js 下载成功"

          # 将 _worker.js 复制回工作目录
          cp -f _worker.js "$GITHUB_WORKSPACE/_worker.js"
          echo "_worker.js 已复制到工作目录"

          # 将最新标签名写入 version.txt (在工作目录)
          echo "${{ env.latest_tag }}" > "$GITHUB_WORKSPACE/version.txt"

          # 清理临时目录
          rm -rf "$TEMP_DIR"
          echo "临时目录已清理"

      - name: 提交更新到 GitHub # 提交更新到 GitHub 仓库
        # 仅在需要更新时执行此步骤
        if: env.need_update == 'true'
        run: |
          # 配置 Git 用户名和邮箱，以便 GitHub Actions 可以提交代码
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          # 添加 _worker.js、version.txt 到 Git 暂存区
          git add _worker.js version.txt
          # 提交更改，提交信息包含更新的版本号
          git commit -m "Update _worker.js to version ${{ env.latest_tag }}"
          # 推送更改到远程仓库
          git push

      # - name: 发送更新成功通知 (复用或创建 GitHub Issue) # 发送更新成功通知 (复用或创建 GitHub Issue)
      #   # 仅在前面的步骤都成功执行且确实进行了更新时触发此通知
      #   if: success() && env.need_update == 'true'
      #   uses: actions/github-script@v8
      #   with:
      #     # 使用 GITHUB_TOKEN 进行身份验证，它具有创建 Issue 的权限
      #     github-token: ${{ secrets.GITHUB_TOKEN }}
      #     script: |
      #       // 获取仓库所有者和名称
      #       const { owner, repo } = context.repo;
      #       // 从环境变量中获取最新标签名和发布类型
      #       const latestTag = process.env.latest_tag;
      #       const releaseType = process.env.release_type;
      #       const typeText = releaseType === 'prerelease' ? '预发布版本' : '正式版本'; // 根据发布类型设置文本
      #       // 定义通知 Issue 的标题和内容
      #       const notificationTitle = `_worker.js 自动更新通知`; // 用于查找和创建的统一标题
      #       const notificationBody = `_worker.js 已成功更新到${typeText} ${latestTag}。`;
      #       const timestamp = new Date().toLocaleString('zh-CN'); // 获取当前时间并格式化

      #       // 尝试查找带有特定标签的现有通知 Issue
      #       const { data: issues } = await github.rest.issues.listForRepo({
      #         owner,
      #         repo,
      #         state: 'open', // 只查找开放的 Issue
      #         labels: 'auto-update-status-issue', // 用于识别此特定通知 Issue 的唯一标签
      #         per_page: 1 // 只获取一个结果
      #       });

      #       let targetIssue = null;
      #       if (issues.length > 0) {
      #         targetIssue = issues[0];
      #       }

      #       if (targetIssue) {
      #         // 如果找到现有 Issue，则在其上添加评论
      #         await github.rest.issues.createComment({
      #           owner,
      #           repo,
      #           issue_number: targetIssue.number,
      #           body: `${notificationBody}\n\n更新时间: ${timestamp}` // 在评论中包含时间戳
      #         });
      #         console.log(`已在现有 Issue #${targetIssue.number} 上添加评论。`); // 打印日志
      #       } else {
      #         // 如果没有找到，则创建新的 Issue
      #         const { data: newIssue } = await github.rest.issues.create({
      #           owner,
      #           repo,
      #           title: notificationTitle, // 使用统一标题
      #           body: `${notificationBody}\n\n更新时间: ${timestamp}`, // 在 Issue 正文中包含时间戳
      #           labels: ['auto-update', 'success', 'auto-update-status-issue'] // 添加所有相关标签，包括唯一标签
      #         });
      #         console.log(`已创建新的 Issue #${newIssue.number}。`); // 打印日志
      #       }

      - name: 初始化环境
        # 仅在需要更新时执行此步骤
        if: env.need_update == 'true'
        run: |
          cd "$GITHUB_WORKSPACE"
          # 安装 wrangler
          npm install

      - name: 创建 KV 命名空间并绑定到 Worker # 执行 step-kv 脚本创建 KV 并绑定
        # 仅在需要更新时执行此步骤
        if: env.need_update == 'true'
        run: |
          cd "$GITHUB_WORKSPACE"

          # 设置执行权限
          chmod +x scripts/step-kv.sh

          # 执行 step-kv 脚本，使用 kv-name 输入参数
          ./scripts/step-kv.sh "${{ env.KV_NAME }}"
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}

      - name: 更新 wrangler.toml 中的 UUID 变量 # 更新 wrangler.toml 中的 UUID 变量
        # 仅在需要更新时执行此步骤
        if: env.need_update == 'true'
        run: |
          cd "$GITHUB_WORKSPACE"

          # 检查 secrets.CF_VAR_UUID 是否设置
          if [ -n "${{ secrets.CF_VAR_UUID }}" ]; then
            echo "正在更新 wrangler.toml 中的 UUID 变量..."

            # 使用 sed 更新 wrangler.toml 中的 UUID 值
            sed -i '/^UUID = ".*"/c\UUID = "'"${{ secrets.CF_VAR_UUID }}"'"' wrangler.toml

            echo "wrangler.toml 中的 UUID 变量已更新"
          else
            echo "secrets.CF_VAR_UUID 未设置，跳过更新"
          fi

      - name: 更新 wrangler.toml 中的 TR_PASS 变量 # 更新 wrangler.toml 中的 TR_PASS 变量
        # 仅在需要更新时执行此步骤
        if: env.need_update == 'true'
        run: |
          cd "$GITHUB_WORKSPACE"

          # 检查 secrets.CF_VAR_TR_PASS 是否设置
          if [ -n "${{ secrets.CF_VAR_TR_PASS }}" ]; then
            echo "正在更新 wrangler.toml 中的 TR_PASS 变量..."

            # 使用 sed 更新 wrangler.toml 中的 TR_PASS 值
            sed -i '/^TR_PASS = ".*"/c\TR_PASS = "'"${{ secrets.CF_VAR_TR_PASS }}"'"' wrangler.toml

            echo "wrangler.toml 中的 TR_PASS 变量已更新"
          else
            echo "secrets.CF_VAR_TR_PASS 未设置，跳过更新"
          fi

      - name: 更新 wrangler.toml 中的自定义域名 # 更新自定义域名配置
        # 仅在需要更新时执行此步骤
        if: env.need_update == 'true'
        run: |
          cd "$GITHUB_WORKSPACE"

          # 检查 secrets.CF_ROUTE_DOMAIN 是否设置（Repository secrets）
          if [ -n "${{ secrets.CF_ROUTE_DOMAIN }}" ]; then
            echo "正在更新 wrangler.toml 添加自定义域名..."

            # 读取当前 wrangler.toml 内容
            CONTENT=$(cat wrangler.toml)

            # 检查是否已有 [[routes]] 配置
            if echo "$CONTENT" | grep -qE '^\[\[routes\]\]'; then
              # 如果已有 routes，删除整个 [[routes]] 部分
              echo "$CONTENT" | sed -E '/^\[\[routes\]\]$/,/^[[:space:]]*$/d' > wrangler.toml.tmp
              mv wrangler.toml.tmp wrangler.toml
            fi

            # 在 wrangler.toml 末尾添加 [[routes]] 配置
            echo '' >> wrangler.toml
            echo '[[routes]]' >> wrangler.toml
            echo 'pattern = "'"${{ secrets.CF_ROUTE_DOMAIN }}"'"' >> wrangler.toml
            echo 'custom_domain = true' >> wrangler.toml

            # 设置 workers_dev 为 false 以使用自定义域名
            sed -i 's/workers_dev = true/workers_dev = false/' wrangler.toml

            echo "wrangler.toml 中的自定义域名已添加"
          else
            echo "secrets.CF_ROUTE_DOMAIN 未设置，跳过自定义域名配置"
          fi

      - name: 部署到 Cloudflare Workers # 部署到 Cloudflare Workers
        # 仅在需要更新时执行此步骤
        if: env.need_update == 'true'
        run: |
          echo "输出 wrangler.toml 内容:"
          cat wrangler.toml

          # 部署 Worker
          npm run deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}

      - name: Delete old workflow runs
        uses: Mattraks/delete-workflow-runs@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          repository: ${{ github.repository }}
          retain_days: 7
          keep_minimum_runs: 1
